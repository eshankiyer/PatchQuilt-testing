[package]
name = "patchquilt-supervisor"
version = "0.1.0"
edition = "2024"
description = "Rust supervisor for tracing a Pumpkin child process"
license = "MIT"

[dependencies]
libc = "0.2"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
